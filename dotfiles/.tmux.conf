set-option -ga update-environment ' PATH'

## Colors
# for i in {0..255}
# do
#    printf "\x1b[38;5;${i}mcolour$i\n"
# done

## Windows
set -g base-index 1
setw -g automatic-rename on

bind-key Right next-window
bind-key Left previous-window
bind-key C-b last-window

# colors
setw -g window-status-current-bg colour9 # ~red
setw -g window-status-current-attr default

## Splitting
unbind %
bind | split-window -h
bind - split-window -v

## Status line
setw -g status-left-length 21
setw -g status-left '[#S]'
setw -g status-right '#(echo $USER) @ #h | %a %d %b (%k:%M)'
# colors
set -g status-bg colour4 # ~blue
# set -g status-fg white

## History
set -g history-limit 10000

## Vim
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection

## Config reload
bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."

# vim: foldmethod=expr foldexpr=getline(v\:lnum)=~'^##'?'>1'\:'='
