# If the message matches no rule it goes to the inbox folder
DEFAULT="$HOME/mail/yahoo"

# TODO: separate
logfile "$HOME/mail/maildrop.log"

xfilter "$HOME/bin/mutt_notif.py"

# Filters
if (/company/)
{
   to "$HOME/mail/work"
}

if (/kurkale6ka/)
{
   to "$HOME/mail/google"
}

if (/mitkofr/)
{
   to "$HOME/mail/yahoo"
}

to "$DEFAULT"
